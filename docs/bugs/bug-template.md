# [BUG] {問題を 1 行で要約 — 重大さ / 影響範囲が伝わる粒度で}

[![MKDN](https://img.shields.io/badge/MKDN-review-red?style=for-the-badge)](https://mkdn.review/?url=https%3A%2F%2Fraw.githubusercontent.com%2Foubakiou%2Fmdxg-redline%2Frefs%2Fheads%2Fmain%2Fdocs%2Fbug%2Fbug-template.md)

> **このファイルはバグ修正プランのテンプレートです**。新しいバグを起票する際は本ファイルをコピーして `docs/bug/bug-<topic>.md` として配置し、`{プレースホルダ}` を埋める / 引用ブロック内のガイドを削除する形で使う。完了したバグ修正は `docs/archive/bug-<topic>.archive.md` にリネームしてアーカイブする（例: `docs/archive/bug-csp-font-src-missing.archive.md`）。
>
> 設計プラン用テンプレートは `docs/feature/feature-plan-template.md`、リファクタリング用テンプレートは `docs/refactoring/refactoring-plan-template.md` を参照。本テンプレートは「既知の不具合 / 設計と実装の乖離 / spec violation / regression」を起票して fix プランを引くための骨格を提供する。

> **書き方の原則**：
>
> - **再現性を優先**: 「修正前にどうなっているか」を読み手が手元で再現できる粒度で書く。DevTools のどの panel / どの API / どの fixture を使うかまで明記する。silent failure 系の bug は再現確認手順そのものが価値の中心
> - **修正範囲を最小化**: 1 ファイル 1 行で済む変更が複数ファイルに波及している場合は、なぜ波及するかを §1 に書く。波及がないなら §4 を 1 段落で済ませる
> - **受け入れ基準は測定可能に**: 「動くようになる」ではなく「Console violation が 0 件になる」「`document.fonts.load(...)` が fulfilled で resolve する」のような客観条件にする。修正前後で観測値が反転することが分かる粒度
> - **コメントは WHY のみ**: bug 発生経緯 / 過去対応の言及はコードコメントには書かない（コメントポリシーは `AGENTS.md`）。本ドキュメント自体が WHY を残す場

{bug の 1 段落要約 — 観測される失敗、影響範囲、root cause を 1 つにまとめる}。

## 1. 問題の構造

> **書く内容**: 「何が壊れているか」を客観的に確認できる形で記録する。「設計ドキュメントと実装の食い違い」「仕様と実装の食い違い」「A と B の不変条件の食い違い」「regression（before / after）」など、対比表が機能する。引用元（DESIGN.md §X、`src/<path>:<line>`、外部仕様 / RFC）を明示してレビュー時の検証経路を残す。
>
> 食い違いではなく単独の logic error / race condition 等の場合は、表ではなく「期待される挙動 / 観測される挙動」の 2 段落構成にする。

{問題の構造を 1-2 段落で説明}。

| 場所                                      | 状態                 |
| ----------------------------------------- | -------------------- |
| {DESIGN.md §X / 外部仕様 / 期待値}        | {仕様が要求する状態} |
| {`src/<path>:<line>`}（{役割}）           | {実装の現状}         |
| {build plugin / CLI rewrite / その他経路} | {仲介経路の状態}     |
| {dist/ 成果物 / runtime 観測}             | {ユーザーに届く状態} |

{追加の引用 / 仕様根拠 があれば}：

> {外部仕様 / DESIGN.md / archive plan からの引用、改行は段落区切りのみ}

## 2. 推定される影響

> **書く内容**: bug が現実にどのような失敗を引き起こすかを具体的に書く。silent failure / 部分的 failure / 環境依存 failure などのカテゴリを意識する。ユーザーから見える / 見えない症状を分けると、なぜ放置されてきたかの説明にもなる。
>
> ブラウザ / OS / 起動経路（`file://` / `http(s)://` / Codespaces）で挙動が分岐する場合は §3 で実機確認を要求するヒントとして明記する。

{失敗パターンの列挙}：

- {失敗パターン 1}: {観測可能な症状 / 観測不可能な症状}
- {失敗パターン 2}: ...
- {silent failure 経路があれば}: {ユーザーに気付かれない理由}

{ブラウザ / 環境差異の注意点があれば 1 段落、§3 で実機検証が必須な根拠としてリンクする}。

## 3. 再現確認手順

> **書く内容**: 「本当にこの bug が発症しているか」を読み手が手元で確認できる手順を順序立てて書く。DevTools のどのタブ / どの API / どの fixture を使うかまで明記する。検証用 fixture が必要なら §3 末尾に inline で並べる。
>
> 手順は「主確認（最も確実な経路）→ 副確認 → 能動確認（API スニペット）→ 目視確認」の優先順で並べると、CI 自動化への移行がスムーズ。Network パネル / Performance / Sources 等、使えない経路があるなら「使えない理由」を末尾の注で明記する（読み手が「そこを見てしまう」のを構造的に避ける）。

1. {初期セットアップ：ビルド / 環境準備}
2. {再現対象を開く / 操作する手順}
3. **主確認 ({DevTools の特定 panel / API})**: {期待される観測}（例: Console に違反メッセージが N 件出る、特定 element が見えない、computed value が想定外、等）
4. **副確認 ({別の経路})**: {期待される観測}
5. **能動確認 (Console から実行)**: {実行コード スニペット} を実行し、{期待される return / reject / 観測値}：

   ```js
   // 検証コード例
   ```

6. **目視確認**: {視覚的な変化 / レイアウト / フォント / 色などの観測点}

注: {DevTools / API の罠 / 使えない経路があれば補足。例: 「Network パネルは inline 化済みリソースを追跡しない」}。

### 検証用 fixture（必要なら）

> 検証に固有 fixture が要る場合は inline で配置。実ファイルとして commit するほどでなければ、本ドキュメント内のコードブロックに留める。

```{ext}
{fixture 本文}
```

### 自動化（Playwright / CI 等、必要なら）

> ヘッドレス環境で同種の bug を検出する経路を 1 段落 + スニペットで書く。本セクションは bug 修正後の regression 防止に直結するため、書ける場合は省略しないことを推奨。

```{lang}
{Playwright / vitest / その他の検出スニペット}
```

## 4. 修正方針

> **書く内容**: 何を変えるかを diff レベルで具体化する。複数ファイルに波及する場合は順序関係を示す。build plugin / CLI rewrite 等で迂回経路を持たせる必要がないか検討する。修正前後を code block で並べると review 時の確認コストが下がる。
>
> 修正と無関係な周辺改変（typo 直し / 命名揺れ / フォーマット差分）は本 PR に混ぜない。混ざる場合はその根拠を明示する。

{修正対象ファイル / 修正内容を 1 段落}。

修正前（現状）：

```{lang}
{現状のコード / 設定 / 文字列}
```

修正後：

```{lang}
{修正後のコード / 設定 / 文字列}
```

{波及範囲 / 不要な周辺変更がないことを 1 段落 で説明。例: 「`src/<path>` が build plugin から参照されているため、build plugin 側の修正は不要」「CLI rewrite 経路は本 file を読まないため CLI 側修正なし」}。

## 5. 受け入れ基準

> **書く内容**: §3 再現確認手順と対称な形で、修正後にどの観測値が反転するかを列挙する。客観的に検証可能な条件にする。「修正前 N 件 → 修正後 0 件」「修正前 reject → 修正後 fulfilled」のような数値 / Promise status / boolean で書ければ理想。

- {修正対象ファイル / 設定}に修正点が反映されていること
- ビルド成果物（{`dist/<path>` 等}）に修正が伝播していること
- §3 検証 fixture / 操作で {主観測値} が {期待値} になる（例: Console に CSP violation が **0 件**）
- §3 能動確認 ({API}) で {期待値}（例: `document.fonts.load(...)` の Promise がすべて `fulfilled`）
- {目視確認の項目があれば}（例: 数式が KaTeX 公式フォントで描画される）
- 既存 `vp test` / `vp check` がすべて通過する（修正による副作用がない）

{受け入れ基準から外す項目があれば 1 段落で。例: 「未使用 family の status が `'unloaded'` のままなのは仕様通りで bug 修正の指標にならない」}。

## 6. テスト追加方針（任意）

> **書く内容**: 同種の drift / regression を構造的に防ぐ test を追加するなら、対象モジュール / 検査内容を 1-2 段落で書く。DESIGN.md / 既存テスト戦略との整合性も併記する。修正のみで対応する場合は本セクションを削除してよい。
>
> in-source test (`if (import.meta.vitest)`) で実装に隣接させる原則は本テンプレートでも維持する。dist 配下の構造検査が必要な場合は DESIGN.md §13「HTML minify 無効維持と CI スモークテスト指針」の方針に従う。

{追加テストの内容を 1-2 段落}：

- `{test 対象モジュール}`: {検査内容 / 期待 invariant}
- `{test 対象モジュール 2}`: ...

{既存テスト戦略との整合性に注意点があれば 1 段落}。

## 7. 関連

> **書く内容**: bug の根拠 / 修正後の影響範囲 / 関連プラン へのリンクを列挙する。設計ドキュメントの章 / 関連 archive plan / 関連 feature plan / 関連 PR が候補。後日読み返すときの出発点。

- [DESIGN.md §{X} {章タイトル}](../design/DESIGN.md#{anchor}) — {bug の根拠 / 期待動作の出典}
- [docs/archive/{関連完了プラン}.archive.md](../archive/{関連完了プラン}.archive.md) — {関連設計判断 / 過去の議論}
- [docs/feature-{影響を受けるプラン}.md](../feature/feature-{topic}.md) — {本 bug 修正後の整合性 / 依存関係}
- {外部仕様 / RFC / W3C / MDN 等の参照、必要なら}
