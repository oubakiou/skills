# {タスク名} 設計・実装計画

[![MKDN](https://img.shields.io/badge/MKDN-review-red?style=for-the-badge)](https://mkdn.review/?url=https%3A%2F%2Fraw.githubusercontent.com%2Foubakiou%2Ftypescript-agent-package-template%2Frefs%2Fheads%2Fmain%2Fdocs%2Ffeature%2Ffeature-plan-template.md)

> **このファイルは設計・実装プランのテンプレートです**。新しい機能や大きめの変更を起票する際は本ファイルを `docs/feature/<topic>.md` としてコピーし、`{プレースホルダ}` を埋める / 引用ブロック内のガイドを削除する形で使う。完了したプランは `docs/archive/<topic>.archive.md` にリネームしてアーカイブする。

> **書き方の原則**:
>
> - **判断の理由を残す**: 何を採用したかだけでなく、なぜ他案を採らなかったかを §5 に残す
> - **DESIGN.md との関係を明示**: 完了時に DESIGN.md のどの章を更新するかを §9 に書く
> - **Step は小さく切る**: 1 PR / 1 セッションで完了できる粒度を 1 Step にする
> - **公開仕様は遅延させない**: README、公開 API、CLI、package exports が変わる Step では同じ commit でドキュメントも更新する

{DESIGN.md の該当章 / README の該当項目} に対応するための設計判断と実装手順をまとめる。完了後は DESIGN.md に永続情報を移し、本ファイルは archive する。

## 1. 対応スコープ

> **書く内容**: このプランで満たす要件を表にする。スコープ外も明示し、レビュー時の議論を絞る。

| 要件              | 現状      | 完了条件               |
| ----------------- | --------- | ---------------------- |
| [MUST] {要件 1}   | 未 / 部分 | {客観的な完了条件}     |
| [MUST] {要件 2}   | 未 / 部分 | {テストまたは観測方法} |
| [SHOULD] {要件 3} | 未        | {実装方針}             |

スコープ外:

- {別タスクで扱う項目}: {理由}

## 2. ベースライン / リファレンス

> **書く内容**: 参考になる既存実装、別プロジェクト、外部仕様がある場合は列挙し、このプロジェクトではどう置き換えるかを書く。ない場合は現行アーキテクチャをベースラインとして説明する。

| 参照元 / 現行実装 | 本実装での扱い             |
| ----------------- | -------------------------- |
| {参照元 1}        | {採用 / 置換 / 不採用理由} |
| {現行実装}        | {変更方針}                 |

## 3. 設計の中核

> **書く内容**: 変更全体を支える設計決定を 1-3 個に絞って書く。データ構造、公開 API、build 出力、設定ファイル、hook、CLI などが候補。

### 3.1 {サブトピック 1}

| 構成要素           | 内容   | 配置 / 寿命                |
| ------------------ | ------ | -------------------------- |
| `{ファイル / API}` | {役割} | {配置 / 生成物 / 公開範囲} |

### 3.2 {サブトピック 2}

{採用する構成を説明する。}

## 4. 実装ステップ

> **書く内容**: 依存関係順に Step を並べる。各 Step は `(未着手)` / `(進行中)` / `(完了済み)` / `(不採用)` の状態マーカーを持つ。完了前に `npm run check` / `npm run test` を通し、必要に応じてサブエージェントで独立レビューする。

### Step 1: (未着手) {設計判断の確定}

- §5 の設計判断をレビュー
- {PoC / 調査 / 仕様確認}

成果物: {確定する判断 / PoC 結果}

### Step 2: (未着手) {純粋ロジック層}

- `src/{module}.ts` に pure 関数を実装
- 同じファイルの `if (import.meta.vitest)` ブロックに正常系 / 境界条件 / 異常系を追加

成果物: {実装ファイルとテスト}

### Step 3: (未着手) {統合層 / 公開 API}

- `src/index.ts` または package exports を更新
- README の公開仕様を同時に更新

成果物: {ユーザーが利用する API / CLI / 設定}

### Step 4: (未着手) {配布物 / package / hook}

- `package.json` / `.agents/scripts/*` / `skills/*/SKILL.md` の必要変更
- `npm run sync-shared:check` で shared/ とコピーの一致を確認
- `gh skill publish --dry-run` で skill 定義を確認

成果物: {配布物 / hook 動作}

### Step 5: (未着手) DESIGN.md 反映と archive 化

- DESIGN.md に永続情報を移す
- 本ドキュメントを `docs/archive/{topic}.archive.md` にリネームする

成果物: DESIGN.md 更新 + archive（archive 化はユーザー確認後）

## 5. 設計判断

> **書く内容**: 論点ごとに候補を比較し、採用 / 不採用の理由を書く。後日同じ議論を繰り返さないための章。

### a. {論点名}

| 候補       | 採用 | 理由         |
| ---------- | ---- | ------------ |
| **{案 A}** | ✓    | {採用理由}   |
| {案 B}     | ✗    | {不採用理由} |
| {案 C}     | ✗    | {不採用理由} |

### b. {論点名}

| 候補       | 採用 | 理由         |
| ---------- | ---- | ------------ |
| **{案 A}** | ✓    | {採用理由}   |
| {案 B}     | ✗    | {不採用理由} |

## 6. テスト方針

> **書く内容**: 自動テストと手動確認を分ける。実装着手時にそのまま test case に落とせる粒度で書く。

### 自動テスト

- `src/{module}.ts` の in-source test
  - {正常系}
  - {境界条件}
  - {異常系}

### 手動確認

- [ ] `npm run check`
- [ ] `npm run test`
- [ ] `npm run sync-shared:check`
- [ ] README の公開仕様と実装が一致している

## 7. 受け入れ基準

- §1 の MUST 要件をすべて満たす
- 既存公開 API が意図せず変わっていない
- 新規挙動に対応するテストがある
- `npm run sync-shared:check` が通る
- DESIGN.md / README が実装と一致している

## 8. 想定リスクと回避策

| リスク                        | 回避策                                          |
| ----------------------------- | ----------------------------------------------- |
| {公開 API の破壊}             | {テスト / README 差分 / semver 判断}            |
| {shared/ とコピーのドリフト}  | `npm run sync-shared:check` で検証              |
| {hook が開発を妨げる}         | `.agents/scripts/check-file.sh` を薄く保つ      |
| {依存追加による install 負荷} | 追加前に標準 API / 既存依存で実現できないか確認 |

## 9. 参考

- [DESIGN.md](../design/DESIGN.md)
- [development.md](../design/development.md)
- {外部仕様 / ライブラリ docs}
